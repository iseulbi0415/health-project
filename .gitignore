### macOS ###
.DS_Store

### Xcode ###
# 각자 컴퓨터에서만 의미 있는 개인 UI 상태(열어둔 파일, 창 배치 등) — 계속 바뀌어서 매번 git에 잡히는 원인
xcuserdata/
*.xcuserstate
DerivedData/

### Secrets ###
# Apple Sign-In 개인키 원본 — base64로 변환해 properties/환경변수로만 다루고, 원본 파일 자체는 절대 커밋 금지
*.p8

### 대회 제출 전 자료 ###
# 발표 전 공개하고 싶지 않은 작업용 초안(SW명세서 자료 등)
대회/작업자료/
