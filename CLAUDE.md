# health-project — 프로젝트 개요 (Claude Code용 컨텍스트)

## 프로젝트 목표
명지대학교 '2026년도 제5회 창의적 SW프로그램 경진대회' SW프로그램 개발 부문 출품작.
역류성 식도염 케어 + 러닝 습관 관리를 위한 개인 건강관리 웹앱.

## 대회 일정
- 참가신청 마감: 7/22
- 완성작품 제출 마감: 8/14
- 1차 서류심사: 8/18~24
- 2차 발표심사: 8/28 (참가자 직접 시연 방식)
- 제출물: SW명세서(PPT), 소스/실행파일, 결과보고서(PPT), 시연영상(mp4, 5분 이내)
- AWS 배포는 제출 요건 아님 (로컬 실행으로 충분)

## 심사 기준 관련 (중요)
선배 조언: 심사는 창의성 > 완성도 중심이며, 코드 한 줄 한 줄보다 전체 아키텍처/설계 이해가 중요.
개발자(본인 또는 AI)가 코드를 짜더라도, 아래는 반드시 본인이 완전히 이해하고 설명할 수 있어야 함:
- 전체 시스템 아키텍처 (프론트-백엔드-DB 구조)
- 사용한 기술적 방법론과 그 이유
- API 명세 (엔드포인트, 요청/응답 형식)
- 사용자 시나리오 (화면 간 흐름)

## 핵심 기능
1. 소화 안전 타이머 — 식사 완료 시 안전하게 누울 수 있는 시간 카운트다운
2. 식단 & 칼로리 기록 — 자주 먹는 음식 빠른 추가 + 총 섭취 칼로리 자동 합산
3. 트리거 음식 경고 — 역류 유발 음식(고지방/튀김, 매운음식, 산성음식, 카페인, 초콜릿, 자극성 향신료, 탄산, 알코올) 표시 (출처: Harvard Health Publishing)
4. 러닝 기록 일지 — 거리, 시간, 페이스, MET 기반 칼로리 계산
5. BMR/TDEE 계산기 (Mifflin-St Jeor Equation 기준)
6. 컨디션 메모장 — 오늘의 컨디션/증상 기록

## 기술 스택
- **프론트엔드**: HTML/CSS/JS (모노레포 루트)
- **백엔드**: Spring Boot (Maven, Java 17, Spring Web + JPA + MySQL Driver + Lombok + Security + OAuth2 Client), `backend/` 서브폴더
- **DB**: MySQL (`health_project`), Homebrew로 설치
- **인증**: 카카오 소셜 로그인 (Spring Security OAuth2 Client, 세션 기반)
- **버전관리**: GitHub (`iseulbi0415/health-project`)
- **에디터**: VS Code(프론트) + IntelliJ(백엔드)

## 현재 진행 상황 (2026-07-19 기준)
- 프론트엔드 완성, D안(리퀴드 글래스 탭형, 하단 탭바 4화면) 디자인 적용 완료
- 백엔드 Food/Run/Memo 3종 Entity+Repository+Controller 완성, CRUD(POST/GET/PUT/DELETE) 전체 구현
- 프론트 전체를 `localStorage` → `fetch` 기반으로 전환 완료
- 롱프레스 편집 UX, 증상 점수 입력 + 증상-식단 인사이트 기능 완료
- **카카오 소셜 로그인(OAuth2) 추가 완료** — 로그인 게이트(비로그인 시 앱 전체를 가리는 전체화면) 방식,
  Food/Run/Memo가 로그인한 사용자 기준으로 분리 저장/조회됨 (`user_id` 연관관계 + 소유권 검증)
  - 실행 전 `backend/src/main/resources/application-secret.properties`에 실제 카카오 REST API 키/Client Secret 입력 필요(git 미포함)
- **로그아웃 즉시 반영 안 되는 버그 수정 완료** — 원인은 CORS 두 겹: ① `/api/auth/logout`은 컨트롤러가 아니라 Security가 직접 처리하는 경로라 `@CrossOrigin`이 안 먹혀서 CORS 헤더 누락 → `SecurityConfig`에 `CorsConfigurationSource` 빈 하나로 통일해 해결. ② 그 후에도 로그아웃 성공 시 서버가 프론트 주소로 리다이렉트를 보내고 있었는데, `fetch`가 이 리다이렉트를 자동으로 따라가다 Live Server(정적 파일 서버, CORS 미지원) 응답을 못 읽어 fetch 자체가 실패 → 로그아웃 성공 핸들러가 리다이렉트 대신 200만 반환하도록 변경해 해결

## 제출 전 정리 백로그 (대회 마감 전 최종 단계에서 처리, 지금 당장 안 해도 됨)
- mockup_D_final.html 등 확정 전 디자인 목업 파일 삭제
- GitHub 저장소 이름 및 로컬 프로젝트 폴더명을 health-project보다 발표용에 어울리는 이름으로 변경
  (변경 시 로컬 git remote URL과 이 CLAUDE.md에 적힌 경로도 같이 수정 필요)
- 추천 타이밍: 7/26~29 디자인 수정 또는 8/1~7 문서화 단계

## 알려진 이슈 / 백로그
- `Memo`는 날짜 필드는 있으나 수정 UI에서 날짜 수정 불가
- "오늘 먹은 음식"(`todayFoods`)이 날짜 구분 없이 계속 쌓이기만 함 — 하루 단위로 관리되도록 개선 필요
- 러닝 칼로리 계산에 임시 체중값(60kg) 대신 실제 입력 체중 연동 필요
- CSS 테마 전환 기능(다크/파스텔) — 추후 구현 예정
- CSRF 보호 비활성화 상태(`SecurityConfig`) — 로컬 데모 범위라 우선 꺼둠, 배포 전 토큰 방식으로 전환 필요
- 로그아웃해도 카카오 자체 로그인 세션(브라우저에 남은 카카오 로그인 상태)까지는 안 끊김 — OAuth2 SSO의 정상 동작이라 버그는 아니지만, 필요하면 매번 강제 재인증시키는 방식을 추가 조사 가능 (우선순위 낮음)
- `NativeKakaoAuthController`도 `AppleAuthController`에서 발견된 것과 동일한 `Map.of` null 위험 패턴 있음 — 카카오 닉네임이 없는 극단적 케이스에서 재현 가능(Apple 로그인 실기기 테스트 중 실제로 이 패턴 때문에 NPE/500 발생해서 발견). 카카오 코드는 이번엔 안 건드렸음, 추후 HashMap으로 통일하는 걸 고려

### 대회(8/28) 종료 후 실서비스 개선 예정 항목
(새 채팅에서 "대회 이후에 하기로 했던 거"라고 물으면 이 목록 기준으로 답할 것)

1. **소화 타이머 실용성 개선**: 4시간 고정이 너무 길게 느껴진다는 피드백. 지방 합산 기반 차등 적용은 이미 되어 있지만, 사용자 체감상 개선 여지 있음. 구체적 방향은 추후 논의.
2. **AI 음식 자동매칭 정확도 개선**: 현재는 자기일관성(3회 병렬 중앙값)으로 이상값 완화만 한 상태. 프롬프트 튜닝 등 정확도 자체를 높이는 작업은 미착수.
3. **DB 기반 그램수 캐싱** (신뢰도 검증 포함) — 자세한 설계는 바로 아래 "DB 기반 그램수 캐싱" 항목 참고, 이 항목과 통합해서 진행
4. **UX 개선**: 끼니 시간순 정렬, 러닝 기록 상대 날짜 표시, 리스트뷰 swipeActions/contextMenu, pull-to-refresh, haptic feedback
5. **온보딩 튜토리얼**
6. **식약처 DB 데이터 품질 개선** — 조리 상태 분류 도입, 다중 소스 교차 검증,
   이상값 탐지. 상세는 아래 "식약처 공공 DB 커버리지·품질 한계" 섹션 참고.

### DB 기반 그램수 캐싱 (대회 종료 후, 실제 운영 전환 시)

**배경**: 대회 제출까지는 메모리 없이 매 요청 3회 병렬 자기일관성
(self-consistency) 방식으로 그램수 추정. 이는 재배포 시 초기화되는
문제가 없고 구현이 간단하지만, 실사용자가 늘어나면 매 요청마다
API 비용(웹검색 포함 시 3배)이 계속 발생하는 구조라 확장성에 한계가
있음. 실제 운영 전환 시 DB 기반 영구 캐싱 도입 필요.

**설계 방향**:
1. 새 음식 첫 검색 시 → 기존처럼 3회 병렬 자기일관성으로 중앙값 계산
2. 이 값을 곧바로 확정 캐시로 저장하지 않고 "검증 대기" 상태로 저장
3. 이후 서로 다른 시점에 몇 차례(예: 3~5회) 더 유사한 값이 나오면
   → "확정" 상태로 전환해 영구 캐시로 사용 (이후 API 호출 없이 재사용)
4. 값이 계속 들쭉날쭉하면 확정하지 않고 계속 재계산

**추가 안전장치 (설계 메모)**:
- 확정된 캐시도 일정 기간(예: 3개월) 후 자동 재검증
- 사용자가 "이 값이 이상해요" 신고할 수 있는 최소 기능
- 관리자가 수동으로 캐시값 수정 가능하게
- 값 변경 시 이전 값 기록(버전 관리)

**전제조건**: 이 작업을 시작하기 전, 앱 출시 후 실제 UX 개선
작업(온보딩 튜토리얼 등)과 함께 진행 — 대회 제출/발표(8/28) 완료
후 착수.

### 식약처 공공 DB 커버리지·품질 한계 (실측 검증 기록, 2026-08-06)

> `sortByRelevance`/`pickClosestToMedian` 로직을 파이썬으로 재현해
> 식약처 API 직접 호출(Claude API·Railway 미경유).
> ⚠️ 아래 값은 전부 100g 기준. 앱 표시값은 1인분 그램수를 곱한 값이므로 다름.

**확인된 한계 두 유형**:
- 유형 1 — 항목이 너무 적음: 곱창. 전체 594건 중 이름 완전일치가 단 1개
  (가공식품 분류). 100g당 100kcal·지방 2.6g으로 상식과 다르나 대안 없음.
- 유형 2 — 조리 상태 혼재: 쌀국수. 완전일치는 많으나 건면·조리식·즉석제품이
  섞여 중앙값이 대표성을 잃음. 선택값 100g당 229kcal·지방 0.6g은 건면(마른 국수)
  기준으로 추정 (앱 표시 300g 기준 환산 시 687kcal·지방 1.8g).
  → 끼니 지방 10g 미만 → 2시간. 실제보다 짧게 안내되는 방향이라 더 위험한 오차.

**검토 후 폐기한 방안**:
- "완전일치 항목을 15개 컷 이전에 전부 수집"
  - 곱창: 변화 없음 (항목이 원래 1개)
  - 쌀국수: 완전일치 15→47건, 그중 42건이 가공식품 → 229→349kcal로 악화
  - 고구마·닭가슴살·시리얼: 완전일치 0건이라 AI 경로, 무영향
  - 삼겹살·신라면: 무변화
  → 판단 기준("기존에 잘 되던 게 깨지면 진행 안 함")에 걸려 진행하지 않음.

**현재 대응 (구현 완료)**:
- 사용자가 소화 체감 카테고리를 직접 바꿔 실측값 오버라이드 가능
- 시연·발표용 음식은 사전에 검색 결과값을 직접 확인해 선정
- 구체적 검색어 사용: "쌀국수"보다 "소고기 쌀국수"가 완성된 요리 항목을
  잡음 — 100g당 151kcal·지방 4.39g(가공식품/상용제품), AI가 추정한
  1인분 그램수(약 240g) 환산 시 362kcal·지방 10g (300g 정액 가정이
  아니라 AI 추정치 기준이라 30g처럼 과한 값이 아님)

**개선 방향 (대회 종료 후)**:
- 조리 상태(생것/건조/조리완료) 구분 분류 도입 검토
- 다중 데이터 소스 교차 검증
- 이상값 탐지 (칼로리는 높은데 지방이 비정상적으로 낮은 패턴 경고)

**채택하지 않기로 한 방향**:
- AI로 영양성분값 전체를 대체: ① AI 추정은 이미 가장 불안정한 구간이고
  ② 출처를 댈 수 없어 건강 앱에서 방어가 어려우며 ③ "AI는 값을 생성하지 않고
  선택·추정만 한다"는 설계 원칙과 정면 충돌.

## 코딩 컨벤션
- 커밋 메시지: `feat:` / `fix:` / `docs:` / `style:` 컨벤션 준수
- 건강/과학 공식(BMR, MET 등) 사용 시 코드 주석 + README에 출처 반드시 기재
- 기존 주석/구조는 유지하고 필요한 부분만 최소 변경 (전체 재작성 지양)
- app.js 구조: ①데이터 / ②HTML요소찾기 / ③함수정의 / ④이벤트리스너 / ⑤초기실행 순서 유지

## README 작성 규칙
- 날짜 제목: `## 진행상황 YYYY-MM-DD`
- 완료 항목: `- [x]` 체크리스트 / "다음에 할 일"은 문장형(체크리스트 아님)
- 오늘 한 일은 핵심 위주로 5~8줄 내외 간결하게
- 본인이 발견/수정한 오류·개선점도 포함
- 기존 날짜 항목과 중복 작성 금지, 새 섹션만 추가

## 작업 시 유의사항
- 이 프로젝트는 학습 목적을 겸하고 있음. 코드를 대신 작성해도 되지만,
  **왜 이렇게 구현했는지, 전체 구조에서 어떤 역할인지는 항상 설명을 남겨줄 것**
  (나중에 발표 준비 시 본인이 다시 읽고 이해할 수 있어야 함)
- 큰 변경사항은 한 번에 처리하기보다, 어떤 파일들을 왜 바꾸는지 먼저 요약하고 진행

## 작업 워크플로우 (반드시 이 순서로 진행)
1. **파악** — 작업 시작 전 관련 파일/코드를 먼저 읽고 현재 상태 파악
2. **계획 제시 (Plan 모드)** — 새 기능/큰 변경은 파일을 바로 고치지 말고, 어떤 파일을 왜 어떻게 바꿀지 계획부터 보여주고 승인 받은 뒤 진행
   (`Shift+Tab`으로 Plan 모드 전환 — 이 프로젝트에서는 새 기능 구현 시 기본으로 이 모드에서 시작할 것)
3. **구현** — 승인된 계획대로 코드 작성/수정
4. **자기 검증** — 에러가 있으면 원인을 분석하고 스스로 고친 뒤 결과를 보고 (막히면 추측으로 우회하지 말고 원인을 설명)
5. **결과 보고** — 뭘 바꿨는지, 왜 그렇게 했는지 요약. 지언이 이해했는지 확인 후 다음 단계로

## 권한 모드 사용 원칙
- 처음에는 파일 수정 전 매번 확인받는 기본 모드(Ask before edit)로 진행
- 익숙해지고 신뢰가 쌓인 반복 작업(예: 이미 검증된 패턴을 다른 화면에도 똑같이 적용하는 것)에 한해 자동 승인 모드로 전환 가능
- **Bypass permissions 모드는 지양** — 검증 안 된 상태에서 전체 권한을 주는 건 위험

## 프롬프트 작성 원칙 (지언이 Claude Code에게 일 시킬 때)
막연하게 "알아서 해줘"라고 하지 말고, 아래를 구체적으로 포함해서 요청:
- **목표**: 최종적으로 뭐가 되어야 하는지
- **현재 상태**: 지금 뭐가 어떻게 되어 있는지 (관련 파일/함수명 포함)
- **제약/맥락**: 지켜야 할 규칙 (위 코딩 컨벤션, 대회 일정 등)

예: "트리거 음식 태그가 화면에 안 뜨는데, Food.java엔 isTrigger 필드 추가했고
app.js의 renderFoodList()에서 태그 표시 코드도 넣었어. 원인 찾아서 고쳐줘"
→ 이렇게 현재 상태 + 이미 한 작업 + 문제를 구체적으로 말하는 방식 유지할 것

## 문서 업데이트 기준
- **README.md**: 매 세션 종료 시 무조건 오늘 날짜 섹션 추가 (README 작성 규칙 참고)
- **ARCHITECTURE.md**: 구조가 실제로 바뀔 때만 업데이트. 판단 기준 —
  새 필드/API 엔드포인트/테이블 추가는 반영 필요, 단순 버그 수정·CSS·텍스트 변경은 불필요.
  세션 종료 전 "이번 작업이 구조를 바꿨는지" 스스로 확인하고, 바뀌었으면 관련 섹션(API 명세/ERD/데이터흐름)을 먼저 업데이트할지 물어볼 것

## 대화 컨텍스트 관리
- 대화가 길어져 컨텍스트가 많이 찼다고 느껴지면 `/clear`로 초기화 후 새로 시작
  (단, 초기화 전 진행 상황을 이 파일이나 README에 먼저 기록해둘 것 — 초기화하면 대화 맥락은 사라지지만 이 CLAUDE.md는 계속 남아있음)
